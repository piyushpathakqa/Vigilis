{
  "name": "@argus/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Vigilis CLI: vigilis author|generate|triage|heal.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "vigilis": "./dist/index.js",
    "argus": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@argus/core": "workspace:*",
    "commander": "^13.0.0"
  }
}
